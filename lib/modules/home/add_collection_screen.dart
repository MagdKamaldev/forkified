import 'package:flutter/material.dart';

class AddCollectionScreen extends StatelessWidget {
  const AddCollectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Add Collection"));
  }
}
